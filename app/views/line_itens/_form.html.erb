<%= form_with(model: line_iten) do |form| %>
  <% if line_iten.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(line_iten.errors.count, "error") %> prohibited this line_iten from being saved:</h2>

      <ul>
        <% line_iten.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :cart_id, style: "display: block" %>
    <%= form.text_field :cart_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
